server:
  host: "0.0.0.0"
  port: 8080
  secret_key: "${SECRET_KEY}"

panel:
  title: "Sanayei Panel"
  theme: "dark"
  language: "fa"

backup:
  enable: true
  interval: 24
  path: "/app/data/backups"
  max_backups: 7
